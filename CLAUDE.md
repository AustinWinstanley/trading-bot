# For AI agents

Read [`AGENTS.md`](AGENTS.md) first. It is the operating contract for this
repository — what will mislead you if you assume a typical project's
conventions, and what actually governs whether an edit is safe to make. This
file exists only so agent tooling that specifically looks for `CLAUDE.md`
finds the same guidance Codex, and any other agent, already reads from
`AGENTS.md`.
